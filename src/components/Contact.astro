---
import { getEntry } from 'astro:content';

// Récupération des paramètres de contact
const contactEntry = await getEntry('settings', 'contact');
const { phoneDisplay, phoneNumber, enableWhatsapp, whatsappNumber, whatsappMessage } = contactEntry.data;

// Création du lien WhatsApp
const whatsappLink = `https://wa.me/${whatsappNumber}?text=${encodeURIComponent(whatsappMessage || '')}`;
---

<section id="contact" class="py-16 bg-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="grid lg:grid-cols-2 gap-12 items-center">
      
      <!-- Colonne Gauche : Procédure (Inchangée) -->
      <div>
        <h2 class="text-3xl font-extrabold text-gray-900 mb-6 font-heading">
          Comment louer votre matériel ?
        </h2>
        <p class="text-lg text-gray-600 mb-8">
          Que vous veniez de Lancieux, Saint-Briac ou d’ailleurs, c'est très simple :
        </p>

        <div class="space-y-6">
          <div class="flex">
            <div class="flex-shrink-0">
              <div class="flex items-center justify-center h-10 w-10 rounded-full bg-emerald-100 text-emerald-600 font-bold text-lg">1</div>
            </div>
            <div class="ml-4">
              <h4 class="text-lg font-bold text-gray-900">Appelez-nous</h4>
              <p class="text-gray-500">Pour réserver votre créneau et vérifier la météo.</p>
            </div>
          </div>
          <div class="flex">
            <div class="flex-shrink-0">
              <div class="flex items-center justify-center h-10 w-10 rounded-full bg-emerald-100 text-emerald-600 font-bold text-lg">2</div>
            </div>
            <div class="ml-4">
              <h4 class="text-lg font-bold text-gray-900"><a target="_blank" href="https://maps.app.goo.gl/NvLCjxuCqW9vGEWF9">Rendez-vous Plage de Saint-Sieu 📍 </a></h4>
              <p class="text-gray-500">Nous sommes installés directement sur la plage.</p>
            </div>
          </div>
          <div class="flex">
            <div class="flex-shrink-0">
              <div class="flex items-center justify-center h-10 w-10 rounded-full bg-emerald-100 text-emerald-600 font-bold text-lg">3</div>
            </div>
            <div class="ml-4">
              <h4 class="text-lg font-bold text-gray-900">On s'occupe de tout</h4>
              <p class="text-gray-500">Préparation du bateau, équipement et mise à l'eau.</p>
            </div>
          </div>
   <!--               <div class="mt-8 p-6 bg-gray-100 rounded-lg">
          <h3 class="font-bold mb-2">📍 Adresse</h3>
          <p>Plage de Saint-Sieu, Lancieux (35)</p>
          <p class="text-sm text-gray-500 mt-2">Bateau de sécurité motorisé toujours à proximité.</p>
        </div>-->

        </div>
      </div>

      <!-- Colonne Droite : Carte Contact DYNAMIQUE -->
      <div class="bg-emerald-900 rounded-2xl p-8 text-white shadow-2xl relative overflow-hidden flex flex-col justify-center h-full min-h-[400px]">
        <!-- Décoration de fond -->
        <div class="absolute -top-10 -right-10 w-40 h-40 bg-emerald-800 rounded-full opacity-50"></div>
        <div class="absolute bottom-0 left-0 w-full h-1/2 bg-gradient-to-t from-emerald-950/50 to-transparent"></div>

        <div class="relative z-10 text-center">
            <h3 class="text-2xl font-bold mb-2 font-heading">Réservez votre session</h3>
            <p class="text-emerald-200 mb-8">Ouvert tous les jours en saison</p>
            
            <!-- Bloc Numéro de téléphone -->
            <div class="mb-6">
                <a href={`tel:${phoneNumber}`} class="block group">
                                <a href={`tel:${phoneNumber}`} class="inline-flex items-center justify-center px-6 py-3 border border-transparent text-base font-bold rounded-full text-emerald-900 bg-white hover:bg-emerald-50 md:py-4 md:text-lg transition transform hover:scale-105 shadow-lg">
                    <span class="text-5xl md:text-4xl font-extrabold tracking-tight group-hover:text-emerald-300 transition duration-300">
                        📞 {phoneDisplay}
                    </span>
                </a>

                </a>
            </div>

            <!-- Boutons d'action -->
            <div class="flex flex-col sm:flex-row gap-4 justify-center mt-8">
                <!-- Bouton Appel Classique -->
<!--                <a href={`tel:${phoneNumber}`} class="inline-flex items-center justify-center px-6 py-3 border border-transparent text-base font-bold rounded-full text-emerald-900 bg-white hover:bg-emerald-50 md:py-4 md:text-lg transition transform hover:scale-105 shadow-lg">
                    <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path></svg>
                    Appeler
                </a>-->

                <!-- Bouton WhatsApp (Conditionnel) -->
                {enableWhatsapp && (
                    <a href={whatsappLink} target="_blank" rel="noopener noreferrer" class="inline-flex items-center justify-center px-6 py-3 border border-transparent text-base font-bold rounded-full text-white bg-[#25D366] hover:bg-[#20bd5a] md:py-4 md:text-lg transition transform hover:scale-105 shadow-lg">
                        <svg class="w-6 h-6 mr-2" fill="currentColor" viewBox="0 0 24 24" aria-hidden="true">
                            <path d="M12.031 6.172c-3.181 0-5.767 2.586-5.768 5.766-.001 1.298.38 2.27 1.019 3.287l-.711 2.598 2.658-.698c.983.586 2.053.896 3.193.898 3.298 0 5.969-2.671 5.969-5.968-.057-6.075-6.36-6.113-6.36-.001zm0-2c4.418 0 8 3.582 8 8s-3.582 8-8 8c-1.397 0-2.709-.356-3.87-1.026l-4.131 1.084 1.102-4.029c-.742-1.259-1.101-2.662-1.101-4.029 0-4.418 3.582-8 8-8z"/>
                        </svg>
                          <span class="text-2xl md:text-2xl font-extrabold tracking-tight group-hover:text-emerald-300 transition duration-300">
                        WhatsApp
                    </span>
                        
                    </a>
                )}
            </div>

            <p class="text-sm text-emerald-300/80 mt-6">
                Bateau de sécurité motorisé toujours à proximité.
            </p>
        </div>
      </div>

    </div>
  </div>
</section>