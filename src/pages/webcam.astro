---
import "../styles/global.css";

import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.vue';
import Contact from '../components/Contact.astro';
import WebcamViewer from '../components/WebcamViewer.vue';
import WeatherWidget from '../components/WeatherWidget.vue';
---

<Layout title="Webcam Live Lancieux | Émeraude Loca-fun">
  <Header client:load />

  <main class="bg-gray-50 min-h-screen">
    
    <!-- En-tête -->
    <div class="bg-emerald-900 text-white py-16">
      <div class="max-w-7xl mx-auto px-4 text-center">
        <h1 class="text-3xl md:text-5xl font-bold font-heading mb-4">La Baie en Direct</h1>
        <p class="text-emerald-200 text-lg">
          Vérifiez l'état de la mer et la météo avant de venir naviguer.
        </p>
      </div>
    </div>

    <!-- Contenu Principal -->
    <section class="max-w-7xl mx-auto px-4 py-12">
      <div class="grid lg:grid-cols-3 gap-8">
        
        <!-- Colonne Principale : La Webcam (2/3) -->
        <div class="lg:col-span-2">
          <WebcamViewer client:visible />
          
          <div class="mt-8 bg-white p-6 rounded-xl border border-gray-100 shadow-sm prose prose-emerald">
            <h3>À propos de ce spot</h3>
            <p>
              Cette vue donne sur <strong>l'Islet</strong>, un point stratégique de la baie de Lancieux. 
              C'est un excellent indicateur pour la marée et l'état du plan d'eau.
            </p>
            <ul>
              <li><strong>Marée haute :</strong> L'Islet est entouré d'eau, idéal pour le Kayak et le Paddle.</li>
              <li><strong>Marée basse :</strong> La plage s'étend à perte de vue, parfait pour le char à voile (en hiver) ou la pêche à pied.</li>
            </ul>
          </div>
        </div>

        <!-- Colonne Droite : Météo (1/3) -->
        <div class="lg:col-span-1">
          <div class="sticky top-24">
            <h3 class="text-xl font-bold text-gray-900 mb-4 font-heading">Conditions actuelles</h3>
            <WeatherWidget client:visible />
            
            <div class="mt-6 bg-blue-50 border border-blue-100 p-4 rounded-lg">
              <h4 class="font-bold text-blue-800 mb-2 flex items-center">
                <span class="text-xl mr-2">💡</span> Conseil du jour
              </h4>
              <p class="text-sm text-blue-700">
                L'image webcam est rafraîchie automatiquement toutes les minutes. 
                Si l'image est noire, il fait probablement nuit ! 🌙
              </p>
            </div>
          </div>
        </div>

      </div>
    </section>

    <Contact />
  </main>
</Layout>